appId: com.apkiosk.apcode
directories:
  output: release
files:
  - dist/**
npmRebuild: false
electronLanguages:
  - en
extraResources:
  - from: ../web/dist
    to: renderer
  - from: ../daemon/dist/apcode-daemon
    to: apcode-daemon
publish:
  provider: github
  owner: masmeert
  repo: code
mac:
  target:
    - dmg
    - zip
  icon: resources/icon.icns
  category: public.app-category.developer-tools
  identity: "Massimo Meert (3942GST99F)"
  hardenedRuntime: true
  entitlements: resources/entitlements.mac.plist
  entitlementsInherit: resources/entitlements.mac.plist
  notarize: true
